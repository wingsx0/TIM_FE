import React from "react";

const HeaderAccount = ({ children }: { children: string }) => {
  return <p className="text-center text-2xl font-semibold mb-10">{children}</p>;
};

export default HeaderAccount;
